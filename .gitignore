# Reference templates (local only)
zzuthesis/
thuthesis/

# HTU thesis test instances (real-thesis experimental dirs — NEVER git-add; memory htuthesis-v1-0-0-test-instance)
# Machine-enforces the "never git-add the test instance" policy (convention alone is insufficient).
htuthesis-v1.0.0/
htuthesis-v1.1.1/
htuthesis-v1.*/

# Reference theses (local only, not for distribution) — all PDFs at repo root
/*.pdf

# Project-local conventions (not for distribution)
CLAUDE.md

# BMAD framework
_bmad/
_bmad-output/

# LaTeX generated files
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.run.xml
*.toc
*.xdv
*.synctex.gz
*.nav
*.snm
*.vrb

# ATDD / debug temp artifacts (inject-test sed-copies)
.atdd-*
.debug-main*

# Compiled thesis PDF (deliverable, regenerated by latexmk)
htuthesis/main.pdf

# Backup files
*.bak

# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Editor files
*.swp
*.swo
*~

# Claude Code
.claude/
