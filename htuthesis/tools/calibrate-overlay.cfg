%% tools/calibrate-overlay.cfg — 可选 calibrate.tex overlay（Story 4.3；AC-5 \InputIfFileExists 真分支示例）
%%
%% calibrate.tex 通过 \InputIfFileExists{calibrate-overlay.cfg}{}{} 加载本文件。存在时覆盖标尺颜色；
%%   缺失时 calibrate.tex 的 \providecommand 兜底（默认 blue/black）—— 优雅降级，编译不受影响。
%% 演示用：把 body 框改为绿色。删本文件或改名即可恢复默认蓝色（ATDD-4.3-I06 测试缺失分支）。
\renewcommand{\calibratebodycolor}{green}
